[project]
name = "kalshi-btc15m-bot"
version = "0.1.0"
description = "Starter scaffold for a Kalshi BTC 15-minute market bot"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
